pipeline {
    agent any
    environment {
        VERSION = '1.1.0'
        RELEASE_VERSION = 'R.2'
    }
    stages {
        stage('Audit tools') {
            steps {
                sh '''
                  git version
                  java -version
                  mvn -version
                '''
            }
        }

        stage('Unit Test') {
            steps {
                dir('./java-tomcat-sample') {
                    sh '''
                        echo "Executing Unit Tests..."
                        mvn test
                    '''
                }
            }
        }

        stage('Build') {
            steps {
                echo "Building version: ${VERSION} with suffix: ${RELEASE_VERSION}"
                echo 'Mention your Application Build Code here!!!'
                dir('./java-tomcat-sample') {
                        sh '''
                            mvn clean install -Dproject.version=2.0.0
                        '''
                    }
            }
        }
    }

        post{
        always {
             cleanWs()
        }
    }
}
